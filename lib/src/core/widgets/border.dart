import 'package:flutter/widgets.dart';

/// A border that is mostly square but with slightly rouned corners.
final roundedSquareBorder = RoundedRectangleBorder(
  borderRadius: BorderRadius.circular(7),
);
